language: python
python:
  - "2.7"
install:
  - pip install -r requirements.txt
script:
  - cwd=$(pwd)
  - cd ${cwd}/src/
  # run Model Test
  - python ModelTest.py
  # run Player Test
  - python PlayerTest.py
